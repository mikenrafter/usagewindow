agent:
  - claude-code
  - codex

# Kasetto may override this destination when installing the integration files.
destination: ~/.config/kasetto

skills:
  - source: ./agent-integration/skills
    skills:
      - usagewindow-status

commands:
  - source: ./agent-integration/commands
    commands:
      - uw-mark-resume

mcps:
  - source: ./agent-integration/mcp/usagewindow
    mcps:
      - usagewindow
